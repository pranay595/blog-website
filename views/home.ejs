<%- include("./partials/header")  %> 
<h1>Home</h1>
<p><%= homeStartingContent %> </p>
<% blogs.forEach(blog => { %>
    <h1><%= blog.title  %> </h1>
    <% if(blog.body.length > 100){ %> 
    <p><%= blog.body.substring(0,98) %> ... <a href="/blogs/<%= blog.title %>">Read more</a></p>
    <% } %> 
    <% }); %> 
<%- include("./partials/footer")  %> 